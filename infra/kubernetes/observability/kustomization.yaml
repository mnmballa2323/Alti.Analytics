apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: observability

resources:
  # In a real environment, these points to remote Helm repo kustomizations
  # https://github.com/prometheus-community/helm-charts
  # - github.com/prometheus-operator/kube-prometheus?ref=main
  - ns.yaml

# Base components for Alti.Analytics observability 
#   - Prometheus (Metrics)
#   - Grafana (Dashboards)
#   - Jaeger (Distributed Tracing)
#   - OpenSearch / ELK (Logs) -> Usually replaced by GCP Cloud Logging natively 
