apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: istio-system

resources:
  - ns.yaml
  - opa-policies.yaml

# Istio setup for mTLS and Zero-Trust
# OPA (Open Policy Agent) webhook configurations setup
